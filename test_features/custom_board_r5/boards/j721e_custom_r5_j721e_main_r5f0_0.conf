# Zephyr SDK does not provide newlib for Cortex-R5; fall back to picolibc
CONFIG_NEWLIB_LIBC=n
CONFIG_PICOLIBC=y

# No USB peripheral wired up on this board
CONFIG_USB_DEVICE_STACK=n
CONFIG_USB_CDC_ACM=n
